[project]
name = "storage"
version = "0.1.0"
description = "Storage adapters: local filesystem now, S3-compatible later"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/storage"]
